# Doc images

README screenshots live here. Drop the budget captures (attached in the chat
where they were taken) with these exact names so the README links resolve:

| File | What it shows | Capture |
|------|---------------|---------|
| `budget-set.png`    | Config tab → Budgets editor (limit, period, 80%/100% thresholds, per-agent rows, Save) | project **Config** tab |
| `budget-usage.png`  | Insights → Budget card: over-budget red bar + healthy per-agent bar | project **Insights** tab |
| `budget-alerts.png` | Notification center (bell) with "Budget exceeded" entries | sidebar bell, opened |

Captured with `?sidebar=collapsed` for a tidy frame.
